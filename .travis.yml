language: php

php:
    - 7.0


sudo: false

matrix:
  fast_finish: true
  allow_failures:
    - php: hhvm

cache:
  - apt

addons:
  apt:
    packages:
      - nginx
      - realpath

install:
#  - composer install
  - travis/install-nginx.sh

script:
  - echo $TRAVIS_COMMIT_RANGE

