{% set organizationInfo = oropro_search_entity_organization_info(entity) %}
{% if organizationInfo is not null %}
    <li>
        {{ 'oro.organization.entity_label'|trans }}: {{ organizationInfo }}
    </li>
{% endif %}
