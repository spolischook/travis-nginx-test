parameters:
    oro_search.elastic_search_mapper.class: OroPro\Bundle\ElasticSearchBundle\Engine\ElasticSearchMapper

services:
    oro_search.elastic_search_mapper:
        class: %oro_search.elastic_search_mapper.class%
        arguments: ["@service_container", %oro_search.entities_config%]
