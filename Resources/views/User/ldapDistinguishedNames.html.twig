{% if value.mappings is not empty %}
    <ul>
        {% for mapping in value.mappings %}
            <li><strong>{{ mapping.name }}:</strong>&nbsp;{{ mapping.dn }}</li>
        {% endfor %}
    </ul>
{% else %}
    {{ 'N/A'|trans }}
{% endif %}
