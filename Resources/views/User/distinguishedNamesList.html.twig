{% if mappings is not empty %}
    <ul>
        {% for mapping in mappings %}
            <li><strong>{{ mapping.name }}:</strong>&nbsp;{{ mapping.dn }}</li>
        {% endfor %}
    </ul>
{% else %}
    {{ 'N/A'|trans }}
{% endif %}
